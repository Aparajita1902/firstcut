ANTHROPIC_API_KEY=

# Hard per-deck cost ceiling (USD) across ALL stages combined. This is the binding
# limit — the run aborts once cumulative spend crosses it, regardless of stage.
TOTAL_BUDGET_USD=8.0

# Per-stage hard cost ceilings (USD). Secondary guards beneath the total cap above.
# Each stage cancels mid-flight if it exceeds its cap, so a runaway loop or retry
# storm can't drain the credit balance.
# Stage 1 = research (web search + pause_turn continuations).
# Stage 2 = inference (candidates + regenerations + sections + synthesis, one key).
# Stage 3 = deck assembly (per-slide Sonnet rendering).
STAGE_1_BUDGET_USD=2.0
STAGE_2_BUDGET_USD=5.0
STAGE_3_BUDGET_USD=4.0
